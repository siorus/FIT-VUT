# Dirstat - statistics of directory
## Overview

This program recursively investigates directories and makes report. You can give it regex which will be use to ignore matching dirs/files. See synopsis in source code.